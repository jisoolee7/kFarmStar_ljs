<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kfarmstar.admin.mapper.AdMapper">

	<select id="getAdPriceList" resultType="AdPrice">
		/* 광고 단가 목록 조회 */
		
		SELECT 
			a.ad_price_code			AS adPriceCode
			,a.ad_cate				AS adCate
			,a.ad_price_per_day		AS adPricePerDay
			,a.member_id			AS memberId
			,a.entry_time			AS regDate
		FROM 
			ad_price AS a

		ORDER BY a.ad_price_code;
		
	</select>


</mapper>